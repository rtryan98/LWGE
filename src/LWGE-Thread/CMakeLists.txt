SET(LWGE_THREAD_NAME "LWGE-Thread")
MAKE_LWGE_STATIC_LIB(${LWGE_THREAD_NAME})
TARGET_LINK_LIBRARIES(${LWGE_THREAD_NAME} PUBLIC
    LWGE-Common)
TARGET_INCLUDE_DIRECTORIES(${LWGE_THREAD_NAME} PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/../../thirdparty/concurrentqueue)
